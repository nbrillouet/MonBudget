import { IMonthYear } from "../generics/date-time.model";

export class FilterPlanTracking {
    idPlan: number;
    monthYear: IMonthYear;

    constructor () {
        // this.monthYear = new IMonthYear();
    }
}